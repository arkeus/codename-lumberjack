package com.arc.lumberjack.world {
	/**
	 * Basic class for tile properties, so that we can easily make changes to the tile size
	 * and have it reflected throughout the program immediately, without having to change the
	 * code in many places.
	 *  
	 * @author Arkeus
	 * 
	 */	
	public class Tile {
		public static const WIDTH:uint = 4;
		public static const HEIGHT:uint = 4;
	}
}